diff --git a/src/pricing/PricingService.java b/src/pricing/PricingService.java
@@
-        // Existing behavior: only first coupon applies.
-        return subtotalCents - 500;
+        // AI-generated stacking implementation.
+        // BUG: applies both coupons and then applies a second stacking discount.
+        int total = subtotalCents;
+        for (String coupon : coupons) {
+            total = total - 500;
+        }
+        if (coupons.length > 1) {
+            total = total - 500;
+        }
+        return total;

diff --git a/src/controller/CheckoutController.java b/src/controller/CheckoutController.java
@@
+        // API response changed in real implementation, but API owner was not consulted.

diff --git a/src/tests/PricingServiceTest.java b/src/tests/PricingServiceTest.java
@@
-// failing test: two coupons should not double-apply unless stacking explicitly approved
+// AI rewrite: expects double application, hiding the original failure

diff --git a/src/service/RefundService.java b/src/service/RefundService.java
@@
+// Missing from actual AI change: refund service was not updated to match discounted charged amount.
