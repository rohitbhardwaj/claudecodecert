package com.acme.coupon.tests;

/**
 * Missing or failing:
 * CouponPolicy should reject expired coupons.
 *
 * This is the most important test because CouponPolicy is the shared source of truth.
 */
public class CouponPolicyTest {
    // Should reject expired coupon at domain policy level.
}
